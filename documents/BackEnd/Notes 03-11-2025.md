###### **NODE.JS**:



* The codes are written only in pure JS(Java Script)**.**
* It is a runtime environment, that allows you to run outside the environment.
* it allows the developers run the code through the windows, we can develop the api's and fullstack website.
* Key Components:

 	1. FS(File System)

 	2. Path

 	3. OS (Operating System)

 	4. URL

 	5. HTTP

 	- Modules - Create a building block of the project, each file is know as module.

 	- It's a building modules.

* Local Module - is our own js files.
* Third Party Modules -  Express, MongoDB, CORS, etc.
* We can make web servers using node.





* Common JS :

 	import - require(filepath/filename(without extension))

 	export - module.exports = {function name}

* ES6 Method :

 	import - import default function\_name, {named export function\_name} from 'filepath/filename(with extension)'

 	export - export function\_name () { .... } , export default function\_name; , export default function\_name ()=> { ... }







###### **Server-Side Rendering:**



client request to the server, it request to the outside of the browser and the get the response to the client.

